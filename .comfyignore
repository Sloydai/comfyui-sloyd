# Never ship credentials in the published archive.
sloyd_config.json

# Development-only files.
tests/
scripts/
.github/
*.md
!README.md

# Listing artwork is fetched by the Registry from the raw GitHub URLs in
# pyproject.toml, so it does not need to ship in every user's install.
assets/

# Local review page.
REVIEW.html
review_assets/
registry_rereview_request.md
